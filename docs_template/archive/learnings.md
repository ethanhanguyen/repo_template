# Archived Learnings

Accumulated project knowledge across sessions. Log entries at session close.

## Format

```markdown
## Session {N}: {date} — {brief description}

### Decisions made
- {decision 1}
- {decision 2}

### Risks flagged
- {risk 1} — {mitigation}

### Anti-patterns to avoid
- {anti-pattern 1}

### Useful patterns
- {pattern 1}

### Bugs found
- `{file}:{line}` — {symptoms} → {root cause} → {fix}

### Gotchas
- {gotcha 1}
```

## Sessions

<!-- Entries accumulate below. Keep them reverse chronological. -->

### Template session (example)

**Session 1: YYYY-MM-DD — Initial project setup**

**Decisions made:**
- Use {tech_A} over {tech_B} because {reason}

**Risks flagged:**
- {risk_description} — mitigate by {mitigation}

**Anti-patterns to avoid:**
- Don't {bad_pattern}, instead {good_pattern}

**Useful patterns:**
- When doing {task}, always {pattern}

**Bugs found:**
- `src/{path}`:{line} — {symptom} caused by {cause}, fixed by {fix}

**Gotchas:**
- {framework} version {X} requires {workaround}
